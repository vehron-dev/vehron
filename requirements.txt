# Runtime dependencies for local/manual environments.
# The package metadata in pyproject.toml is the authoritative install contract.
numpy>=1.24
pydantic>=2.0
pyyaml>=6.0
matplotlib>=3.7
click>=8.1

# Optional
scipy>=1.10
pandas>=2.0
coolprop>=6.6
pybamm>=23.0
streamlit>=1.28
gpxpy>=1.5
